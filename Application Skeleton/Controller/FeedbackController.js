const FeedbackController = () => {
    /**
     * Handles sending contact Feedbacks
     * Logs the contact information and resets the form fields.
     *  
     * @param {Object} Feedback - The contact information
     * @param {Function} resetForm - Function to reset the form fields
     */
    const handleSendFeedback = (Feedback, resetForm) => { }     
}

export default FeedbackController;

  